Trigger the one-shot PR workflow that persists the same-session stale-source guard to main.
Retry after limiting the bot commit to non-workflow files.
Render patched workflow copy for connector persistence.
