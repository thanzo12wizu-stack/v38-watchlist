name: Dynamic pioneer follow-through research

on:
  push:
    branches:
      - research/sector-stock-leadership-20260826
    paths:
      - leadership/research/validate_dynamic_pioneer_followthrough.py
      - leadership/research/dynamic_followthrough_fast_patch.py
      - .github/workflows/dynamic-pioneer-followthrough-research.yml

permissions:
  contents: read

jobs:
  validate:
    strategy:
      fail-fast: false
      matrix:
        include:
          - tranche: t1
            exclude_first: 0
          - tranche: t2
            exclude_first: 1500
          - tranche: t3
            exclude_first: 3000
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - name: Install research dependencies
        run: python -m pip install --disable-pip-version-check pandas numpy scipy yfinance
      - name: Syntax check
        run: |
          python -m py_compile leadership/research/validate_dynamic_pioneer_followthrough.py
          python -m py_compile leadership/research/dynamic_followthrough_fast_patch.py
      - name: Run fixed dynamic follow-through
        run: |
          python leadership/research/dynamic_followthrough_fast_patch.py \
            --root . \
            --output leadership/research/dynamic_pioneer_${{ matrix.tranche }} \
            --analysis-start 2016-01-04 \
            --analysis-end 2026-06-20 \
            --exclude-first ${{ matrix.exclude_first }} \
            --max-tickers 1500 \
            --batch-size 75 \
            --min-members 3
      - name: Upload research outputs
        uses: actions/upload-artifact@v4
        with:
          name: dynamic-pioneer-${{ matrix.tranche }}
          path: leadership/research/dynamic_pioneer_${{ matrix.tranche }}/
          retention-days: 7
