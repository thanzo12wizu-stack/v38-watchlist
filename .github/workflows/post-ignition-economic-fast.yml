name: Post ignition economic fast

on:
  workflow_dispatch:
  push:
    branches:
      - research/sector-stock-leadership-20260826
    paths:
      - .github/workflows/post-ignition-economic-fast.yml

jobs:
  validate:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check pandas numpy scipy yfinance
      - name: Run focused economic test
        run: |
          python leadership/research/validate_post_ignition_economic_fast.py \
            --root . \
            --output leadership/research/post_ignition_economic_fast \
            --analysis-start 2022-01-03 \
            --analysis-end 2026-06-30 \
            --max-tickers 1500 \
            --batch-size 75 \
            --min-members 3
      - name: Upload outputs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: post-ignition-economic-fast
          path: leadership/research/post_ignition_economic_fast/
          if-no-files-found: warn
          retention-days: 7
