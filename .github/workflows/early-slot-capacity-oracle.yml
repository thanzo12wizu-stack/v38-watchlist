# Research-only. Does not modify main, Pages, or dashboard outputs.
name: Early Slot Capacity Oracle

on:
  push:
    branches:
      - research/leader-entry-delay-audit-20260904
    paths:
      - leadership/research/audit_early_slot_capacity_oracle.py
      - .github/workflows/early-slot-capacity-oracle.yml
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-24.04
    timeout-minutes: 150
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - name: Install research dependencies
        run: python -m pip install --disable-pip-version-check pandas numpy scipy yfinance
      - name: Syntax check
        run: |
          python -m py_compile leadership/research/audit_early_slot_capacity_oracle.py
          python -m py_compile leadership/research/audit_major_leader_entry_delay.py
      - name: Run Early slot capacity oracle
        env:
          PYTHONPATH: .:leadership/research
        run: |
          python leadership/research/audit_early_slot_capacity_oracle.py \
            --root . \
            --output leadership/research/early_slot_capacity_oracle_output \
            --analysis-start 2016-01-04 \
            --analysis-end 2026-09-02 \
            --max-tickers 6000 \
            --batch-size 75
      - name: Upload research outputs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: early-slot-capacity-oracle
          path: leadership/research/early_slot_capacity_oracle_output/
          retention-days: 14
