# Research-only. Does not modify main, Pages, dashboard, or production logic.
name: RS Horizon Leader Capture Research

on:
  push:
    branches:
      - research/sector-stock-leadership-20260826
    paths:
      - leadership/research/audit_rs_horizon_leader_capture.py
      - leadership/research/run_rs_horizon_leader_capture.py
      - .github/workflows/rs-horizon-leader-capture-research.yml
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - name: Install research dependencies
        run: python -m pip install --disable-pip-version-check pandas numpy scipy yfinance
      - name: Syntax check
        run: |
          python -m py_compile leadership/research/audit_rs_horizon_leader_capture.py
          python -m py_compile leadership/research/run_rs_horizon_leader_capture.py
      - name: Run RS horizon leader capture audit
        env:
          PYTHONPATH: .:leadership/research
        run: |
          python leadership/research/run_rs_horizon_leader_capture.py \
            --root . \
            --output leadership/research/rs_horizon_leader_capture \
            --analysis-start 2020-01-02 \
            --analysis-end 2026-09-02 \
            --leader-start 2021-01-04 \
            --max-tickers 6000 \
            --batch-size 75
      - name: Upload research outputs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rs-horizon-leader-capture
          path: leadership/research/rs_horizon_leader_capture/
          retention-days: 14
