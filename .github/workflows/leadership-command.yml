name: Leadership Command isolated build

on:
  pull_request:
    paths:
      - 'leadership/**'
      - '.github/workflows/leadership-command.yml'
  push:
    branches:
      - main
    paths:
      - 'leadership/**'
      - '.github/workflows/leadership-command.yml'
  schedule:
    # Existing dashboard runs independently at UTC 22:17 and 04:17.
    # Leadership starts later but has no needs/dependency/writeback to that flow.
    - cron: '10 23 * * 1-5'
    - cron: '10 5 * * 2-6'
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: leadership-command-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 40
    env:
      TZ: Asia/Tokyo
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 1

      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'

      - name: Install isolated Leadership dependencies
        run: python -m pip install --disable-pip-version-check pandas numpy yfinance

      - name: Unit tests
        run: python -m unittest discover -s leadership/tests -p 'test_*.py' -v

      - name: Build independent market snapshot
        shell: bash
        run: |
          set -euo pipefail
          MAX_SYMBOLS=0
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            # PR validates the live data path without paying for a full-universe run.
            MAX_SYMBOLS=400
          fi
          python leadership/build_market_snapshot.py \
            --universe universe.csv \
            --output leadership/market_snapshot.json \
            --benchmark QQQ \
            --period 15mo \
            --batch-size 80 \
            --max-symbols "$MAX_SYMBOLS"

      - name: Build Leadership Command
        run: python leadership/build_leadership.py --root . --output leadership/dist

      - name: Validate live coverage
        shell: bash
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          snap=json.loads(Path('leadership/market_snapshot.json').read_text())
          diag=json.loads(Path('leadership/dist/diagnostics.json').read_text())
          cov=diag.get('coverage', {})
          print(json.dumps({
            'snapshot_asof': snap.get('asof'),
            'requested': snap.get('universe_requested'),
            'valid': snap.get('universe_valid'),
            'metric_source': cov.get('metric_source'),
            'rs63': cov.get('rs63'),
            'entry_inputs': cov.get('entry_inputs'),
            'confidence': cov.get('confidence'),
            'top_level_keys': diag.get('top_level_keys'),
          }, ensure_ascii=False, indent=2))
          if cov.get('metric_source') != 'leadership/market_snapshot.json':
            raise SystemExit('Leadership builder did not use its independent market snapshot')
          if int(cov.get('rs63') or 0) < 30:
            raise SystemExit('Insufficient RS coverage')
          if int(cov.get('entry_inputs') or 0) < 30:
            raise SystemExit('Insufficient entry-metric coverage')
          PY

      - name: Upload preview artifact
        uses: actions/upload-artifact@v4
        with:
          name: leadership-command-preview
          path: leadership/dist/
          if-no-files-found: error
          retention-days: 7

  publish:
    if: github.event_name != 'pull_request'
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    env:
      LEADERSHIP_PUBLIC_REPOSITORY: ${{ vars.LEADERSHIP_PUBLIC_REPOSITORY }}
      LEADERSHIP_PUBLIC_TOKEN: ${{ secrets.LEADERSHIP_PUBLIC_TOKEN }}
    steps:
      - name: Check isolated public target
        id: config
        shell: bash
        run: |
          if [[ -z "${LEADERSHIP_PUBLIC_REPOSITORY:-}" || -z "${LEADERSHIP_PUBLIC_TOKEN:-}" ]]; then
            echo '::notice::Dedicated Leadership public repository is not configured; existing site is untouched.'
            echo 'enabled=false' >> "$GITHUB_OUTPUT"
          elif [[ ! "$LEADERSHIP_PUBLIC_REPOSITORY" =~ ^[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+$ ]]; then
            echo 'Invalid LEADERSHIP_PUBLIC_REPOSITORY'
            exit 1
          else
            echo 'enabled=true' >> "$GITHUB_OUTPUT"
          fi

      - uses: actions/download-artifact@v4
        if: steps.config.outputs.enabled == 'true'
        with:
          name: leadership-command-preview
          path: /tmp/leadership-site

      - name: Publish only to dedicated repository
        if: steps.config.outputs.enabled == 'true'
        shell: bash
        run: |
          set -euo pipefail
          target=/tmp/leadership-pages
          git clone --depth 1 "https://x-access-token:${LEADERSHIP_PUBLIC_TOKEN}@github.com/${LEADERSHIP_PUBLIC_REPOSITORY}.git" "$target"
          find "$target" -mindepth 1 -maxdepth 1 ! -name .git -exec rm -rf -- {} +
          cp -a /tmp/leadership-site/. "$target/"
          touch "$target/.nojekyll"
          cd "$target"
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          if git diff --staged --quiet; then
            echo 'Leadership public site already current'
            exit 0
          fi
          git commit -m 'Publish Leadership Command'
          git push origin HEAD:main
