name: One-off inspect MC15 implementation targets

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/inspect-mc15-impl-once.yml'

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 1
      - name: Print MC state migration targets
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path
          lines=Path('build_dashboard.py').read_text(encoding='utf-8',errors='replace').splitlines()
          for a,b in [(5000,5385),(8260,8310),(21290,21345)]:
              print(f'\n=== {a}-{b} ===')
              for n in range(a,min(b,len(lines))+1): print(f'{n}: {lines[n-1]}')
          PY
