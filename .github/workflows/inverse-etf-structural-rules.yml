name: Inverse ETF Structural Rules

on:
  workflow_dispatch:
  push:
    branches: [research/inverse-etf-regime-scan-20260902]
    paths:
      - leadership/research/audit_inverse_structural_rules.py
      - .github/workflows/inverse-etf-structural-rules.yml

permissions:
  contents: read
  actions: read

jobs:
  audit:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check pandas numpy yfinance
      - name: Download base inverse scan artifact
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p inputs/base
          gh api repos/$GITHUB_REPOSITORY/actions/artifacts/9850106810/zip > /tmp/base.zip
          unzip -q /tmp/base.zip -d inputs/base
          test -s inputs/base/inverse_feature_state.csv.gz
      - name: Run structural rules
        run: |
          python leadership/research/audit_inverse_structural_rules.py \
            --base inputs/base/inverse_feature_state.csv.gz \
            --output leadership/research/out/inverse_structural_rules \
            --start 2016-01-04 \
            --end 2026-03-20
      - name: Upload outputs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: inverse-etf-structural-rules
          path: leadership/research/out/inverse_structural_rules/
          if-no-files-found: warn
          retention-days: 14
