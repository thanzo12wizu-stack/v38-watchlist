name: TQQQ backtest run once

on:
  push:
    branches: [tqqq-backtest-once-20260826]
    paths:
      - '.github/workflows/tqqq-backtest-run-once.yml'
      - 'research/tqqq_stage18_regime_sized_reentry.py'
      - 'research/tqqq_stage17_hierarchy_crisis_fix.py'
      - 'research/tqqq_stage16_adversarial_bear_stress.py'

permissions:
  contents: read
  actions: read

jobs:
  stage18:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: pip install --disable-pip-version-check -r requirements.txt
      - name: Run regime-sized reentry stress study
        shell: bash
        run: |
          set -euo pipefail
          PYTHONPATH=. python research/tqqq_stage18_regime_sized_reentry.py 2>&1 | tee tqqq_stage18.log
      - name: Upload Stage18 results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tqqq-stage18-regime-sized-reentry
          if-no-files-found: warn
          path: |
            tqqq_stage18.log
            tqqq_stage18_summary.json
            tqqq_stage18_summary.csv
            tqqq_stage18_ranked.csv
            tqqq_stage18_mc.csv
            tqqq_stage18_historical.csv
