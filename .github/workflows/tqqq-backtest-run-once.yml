name: TQQQ backtest run once

on:
  push:
    branches: [tqqq-backtest-once-20260826]
    paths:
      - '.github/workflows/tqqq-backtest-run-once.yml'
      - 'research/tqqq_stage13_sub20_integrated.py'
      - 'research/tqqq_stage11_integrated_nqsar.py'

permissions:
  contents: read
  actions: read

jobs:
  stage13:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: pip install --disable-pip-version-check -r requirements.txt
      - name: Run sub-20 integrated NQSAR study
        shell: bash
        run: |
          set -euo pipefail
          PYTHONPATH=. python research/tqqq_stage13_sub20_integrated.py 2>&1 | tee tqqq_stage11.log
      - name: Upload Stage13 results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tqqq-stage13-sub20-integrated
          if-no-files-found: warn
          path: |
            tqqq_stage11.log
            tqqq_stage11_summary.json
            tqqq_stage11_base_grid.csv
            tqqq_stage11_integrated_grid.csv
            tqqq_stage11_robust.csv
