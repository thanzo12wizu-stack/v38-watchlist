name: Leadership Regeneration Cutoffs 20260902

on:
  push:
    branches:
      - research/leadership-cycle-audit-20260902
    paths:
      - leadership/research/audit_leadership_regeneration_cutoffs.py
      - .github/workflows/leadership-regeneration-cutoffs-20260902.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  cutoffs:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
        with:
          ref: research/leadership-cycle-audit-20260902
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
      - run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Run cutoff comparison
        env:
          PYTHONPATH: .:leadership/research
        run: |
          python leadership/research/audit_leadership_regeneration_cutoffs.py \
            --root . \
            --output leadership_regeneration_cutoff_output \
            --analysis-start 2016-01-04 \
            --analysis-end 2026-08-31 \
            --max-tickers 6000 \
            --batch-size 75
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: leadership-regeneration-cutoffs-20260902
          path: leadership_regeneration_cutoff_output/
          retention-days: 30
