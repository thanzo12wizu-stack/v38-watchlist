name: Probe Capture Bottlenecks

on:
  push:
    branches:
      - research/leader-entry-delay-audit-20260904
    paths:
      - leadership/research/audit_probe_capture_bottlenecks.py
      - .github/workflows/probe-capture-bottlenecks.yml
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check pandas numpy scipy yfinance
      - name: Syntax check
        run: python -m py_compile leadership/research/audit_probe_capture_bottlenecks.py
      - name: Run bottleneck decomposition
        env:
          PYTHONPATH: .:leadership/research
        run: |
          python leadership/research/audit_probe_capture_bottlenecks.py \
            --root . \
            --output leadership/research/probe_capture_bottlenecks_output \
            --analysis-start 2016-01-04 \
            --analysis-end 2026-09-02 \
            --max-tickers 6000 \
            --batch-size 75
      - name: Upload outputs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: probe-capture-bottlenecks
          path: leadership/research/probe_capture_bottlenecks_output/
          retention-days: 14
