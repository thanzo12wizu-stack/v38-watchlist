name: Lead lag sequence research

on:
  push:
    branches:
      - research/sector-stock-leadership-20260826
    paths:
      - leadership/research/validate_lead_lag_sequence.py
      - .github/workflows/lead-lag-sequence-research.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - name: Install research dependencies
        run: python -m pip install --disable-pip-version-check pandas numpy scipy yfinance
      - name: Syntax check
        run: python -m py_compile leadership/research/validate_lead_lag_sequence.py
      - name: Run lead lag sequence validation
        run: |
          python leadership/research/validate_lead_lag_sequence.py \
            --root . \
            --output leadership/research/lead_lag_output \
            --analysis-start 2016-01-04 \
            --analysis-end 2026-06-20 \
            --max-tickers 1500 \
            --batch-size 75 \
            --min-members 3
      - name: Upload research outputs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lead-lag-sequence-validation
          path: leadership/research/lead_lag_output/
          retention-days: 7
