name: Archived research bootstrap restart hook

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  status:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: echo "Ten-year research is complete; automatic restart is disabled."
