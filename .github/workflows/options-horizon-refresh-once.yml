name: Options horizon refresh once

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/options-horizon-refresh-once.yml'

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-24.04
    steps:
      - name: Dispatch Options positioning
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run options.yml --repo thanzo12wizu-stack/v38-watchlist --ref main -f force_full_refresh=false
