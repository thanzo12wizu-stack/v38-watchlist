name: Validate restored production chain once

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/restore-production-chain-once.yml'
  workflow_dispatch: {}

permissions:
  contents: read
  actions: write

concurrency:
  group: validate-restored-production-chain-once
  cancel-in-progress: false

jobs:
  validate-and-dispatch:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 1

      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'

      - name: Validate production ownership and wiring
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --disable-pip-version-check pytest
          pytest -q \
            tests/test_atomic_pipeline_workflows.py \
            tests/test_atomic_publish_gate.py \
            tests/test_public_site_export.py

      - name: Dispatch Command Center once
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          gh workflow run dashboard.yml --repo "$GITHUB_REPOSITORY" --ref main
          echo 'Command Center dispatched. Downstream must now start only from its successful workflow_run.'
