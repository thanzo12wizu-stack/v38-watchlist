# Research-only final Early1 audit. Does not modify main, Pages, Dashboard, or live rules.
name: Early1 Operational Final Research

on:
  push:
    branches:
      - research/sector-stock-leadership-20260826
    paths:
      - leadership/research/audit_early1_operational_final.py
      - .github/workflows/early1-operational-final-research.yml
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-24.04
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - name: Install research dependencies
        run: python -m pip install --disable-pip-version-check pandas numpy scipy yfinance
      - name: Syntax check
        run: python -m py_compile leadership/research/audit_early1_operational_final.py
      - name: Run Early1 operational final audit
        env:
          PYTHONPATH: .:leadership/research
        run: |
          python leadership/research/audit_early1_operational_final.py \
            --root . \
            --output leadership/research/early1_operational_final \
            --analysis-start 2016-01-04 \
            --analysis-end 2025-12-31 \
            --max-tickers 6000 \
            --batch-size 75
      - name: Upload research outputs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: early1-operational-final
          path: leadership/research/early1_operational_final/
          retention-days: 14
