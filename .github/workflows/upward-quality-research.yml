name: Upward Quality Research

on:
  push:
    branches:
      - research/sector-stock-leadership-20260826
    paths:
      - leadership/research/discover_upward_quality.py
      - .github/workflows/upward-quality-research.yml
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - name: Install research dependencies
        run: python -m pip install --disable-pip-version-check pandas numpy scipy scikit-learn yfinance
      - name: Syntax check
        run: python -m py_compile leadership/research/discover_upward_quality.py
      - name: Run frozen upward-quality discovery
        run: |
          python leadership/research/discover_upward_quality.py \
            --root . \
            --output leadership/research/upward_quality_outputs \
            --max-tickers 6000 \
            --batch-size 75 \
            --min-members 3
      - name: Upload research outputs
        uses: actions/upload-artifact@v4
        with:
          name: upward-quality-research
          path: leadership/research/upward_quality_outputs/
          retention-days: 7
