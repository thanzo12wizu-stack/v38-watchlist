+<section id="detail" class="panel"><div class="card"><div class="hdr"><h2>市場・TQQQ 詳細</h2><span class="badge">監査用</span></div><div class="row"><div class="mini"><span>Market Mode</span><b id="mode">—</b></div><div class="mini"><span>Breadth / Coverage</span><b id="breadth">—</b></div><div class="mini"><span>新規上限</span><b id="capacity">—</b></div><div class="mini"><span>Strict LOO</span><b id="looStatus">—</b></div></div><div class="row"><div class="mini"><span>Strict Seed (3条件AND)</span><b id="seed">—</b></div><div class="mini"><span>Seed Age</span><b id="seedAge">—</b></div><div class="mini"><span>VIX</span><b id="vix">—</b></div><div class="mini"><span>QQQ SMA50 ATR乖離</span><b id="atrDev">—</b></div><div class="mini"><span>QQQ 10d DD</span><b id="dd10">—</b></div><div class="mini"><span>Stage56 Requested TQQQ</span><b id="requestedTarget">—</b></div><div class="mini"><span>Remaining Hold Days</span><b id="holdDays">—</b></div><div class="mini"><span>MC57 Active Exit</span><b id="mcExit">—</b></div></div></div><div class="card"><div class="hdr"><h2>最終配分の詳細</h2><span class="badge">LIVE</span></div><div class="row"><div class="mini"><span>RSI Reset 希望 / 最終</span><b id="resetAllocation">DATA REQUIRED</b></div><div class="mini"><span>TQQQ 保護 / 追加</span><b id="tqqqParts">DATA REQUIRED</b></div><div class="mini"><span>通常株 希望 / 最終</span><b id="normalAllocation">DATA REQUIRED</b></div><div class="mini"><span>TQQQ 最終 / 合計</span><b id="executableTarget">DATA REQUIRED</b></div></div><div class="note" id="allocationNote">優先順は RSI Reset → TQQQを競合時80%まで保護 → 通常株 → 余りがあればTQQQ追加。合計は100%以内。入力不足時は0%とみなさずDATA REQUIRED。</div></div></section>
 <section id="legacy" class="panel"><div class="card"><div class="hdr"><h2>既存Dashboard（変更なし）</h2><a class="link" href="command-center.html" target="_blank">別画面で開く →</a></div><div class="explain">既存Dashboardの表示・配色・ロジックは変更していません。</div><iframe class="legacy" src="command-center.html" title="Existing Command Center"></iframe></div></section>
 </div>
 <script>
@@ -51,8 +53,11 @@
 function pickCard(x){let q=rankMeta(x),can=STATE.market.mode==='ATTACK'||STATE.market.mode==='SELECTIVE';return`<div class="pick"><div class="rankno">#${q.rank??'—'} ${can?'買付優先順位':'再開時ウォッチ'}</div><div class="ticker">${esc(x.ticker)}</div><div class="pickline">$${fmt(x.price,2)} ／ ${esc(x.peer_theme||'Themeなし')}</div><div class="score">${q.score==null?'DATA REQUIRED':fmt(q.score,1)}</div><div class="pickline">${q.label} Score</div><span class="rotchip">${esc(rotationLabel(x))}</span><div class="why">${esc(whyText(x))}</div></div>`}
 function renderRanking(){let rows=rankRows(),m=STATE.market,r=STATE.ranking||{},entry=m.mode==='ATTACK'||m.mode==='SELECTIVE';let ready=entry?(m.mode==='SELECTIVE'||r.strict_loo_live_status==='READY'):r.attack_watch_status==='READY';$('rankingTitle').textContent=entry?'今日の買付優先順位':'再開したら見る順番';$('rankingSubtitle').textContent=entry?'空き枠がある場合、この順に翌営業日寄りの候補。':'今は買わない。Market Modeが再開した時の候補を先に準備しています。';$('rankingBadge').textContent=ready?'正式ロジックで順位化':'DATA REQUIRED';$('rankStatus').textContent=ready?'順位READY':'DATA REQUIRED';$('quickRankTitle').textContent=entry?'今、優先して見る銘柄':'再開時にまず見る銘柄';$('quickRankExplain').textContent=entry?'現在のModeと空き枠が許す範囲で、上から優先します。':'現在は新規買付不可。下は「もし市場が再開したら何から見るか」で、買い推奨ではありません。';let top=ready?rows.slice(0,3):[];$('quickPicks').innerHTML=top.map(pickCard).join('')||'<div class="empty">正式順位に必要なデータが揃っていません。</div>';$('topPicks').innerHTML=top.map(pickCard).join('')||'<div class="empty">正式順位に必要なデータが揃っていません。</div>';$('candidateRows').innerHTML=rows.map(x=>{let q=rankMeta(x),rot=rotationLabel(x);return`<tr><td>${q.rank??'—'}</td><td><b>${esc(x.ticker)}</b></td><td>${q.score==null?'DATA REQUIRED':fmt(q.score,1)}</td><td>${fmt(x.rs189)}</td><td>${fmt(x.rs63)}</td><td>${esc(x.peer_theme||'—')}</td><td>${x.peer_theme_score==null?'—':fmt(x.peer_theme_score)}</td><td>${esc(rot)}</td><td>${entry?'空き枠があれば候補':'今は買わない'}</td></tr>`}).join('')}
 function renderToday(){if(!STATE)return;let m=STATE.market,held=positions().length,mt=modeText(m);$('modeHuman').textContent=mt[0];$('modeHuman').className='v '+mt[2];$('modeHumanReason').textContent=mt[1];$('nqsarHuman').textContent=m.nqsar||'—';$('nqsarHuman').className='v '+(String(m.nqsar).toLowerCase()==='red'?'bad':String(m.nqsar).toLowerCase()==='yellow'?'warn':'good');$('breadthHuman').textContent=fmt(m.breadth50)+'%';$('todayReason').textContent=humanReason(m);$('mode').textContent=m.mode;$('breadth').textContent=`${fmt(m.breadth50)}% / ${(m.coverage*100).toFixed(1)}%`;$('capacity').textContent=m.new_entry_limit;$('looStatus').textContent=STATE.ranking?.attack_watch_status||STATE.ranking?.strict_loo_live_status||'—';if(m.mode==='DEFENSE'){$('todayAction').textContent='通常株は次回寄りで全退避';$('normalDecision').textContent='新規0・全退避';$('holdingDecision').textContent=held?'次回寄りで全退避':'保有なし'}else if(m.mode==='STOP'){$('todayAction').textContent='今日は通常株を新規で買わない';$('normalDecision').textContent='新規0';$('holdingDecision').textContent=held?`${held}銘柄を個別Exitまで継続`:'保有なし'}else{let cap=Math.max(0,m.new_entry_limit-held);$('todayAction').textContent=cap>0?`通常株を最大${cap}銘柄まで追加可能`:'今日は通常株を追加しない';$('normalDecision').textContent=`最大${m.new_entry_limit}枠`;$('holdingDecision').textContent=held?`${held}銘柄保有`:'保有なし'}$('resumeCondition').textContent=m.mode==='STOP'||m.mode==='DEFENSE'?'Blue/Green＋Breadth≥50%':'現在Entry資格あり';renderRanking()}
+function resetStatusJa(s){return({ACTIVE_POSITION:'保有中',SIGNAL_TODAY_NEXT_OPEN:'反発確認→翌寄り',RSI30_TOUCHED_WAIT_RISE:'30以下到達・反発待ち',APPROACHING_RSI30:'RSI30まで5pt以内',NEAR_RSI30:'RSI30まで10pt以内',WATCHING:'監視中',SIGNAL_OCCURRED:'Signal済み'}[s]||s||'—')}
+function renderResetWatch(){let r=STATE?.panic_reset||{},sum=r.monitor_summary||{},rows=r.monitor||[];$('resetWatchStatus').textContent=r.status==='READY / LIVE'?'LIVE / '+rows.length+'銘柄':'DATA REQUIRED';$('resetActiveCount').textContent=sum.active_positions??r.position_count??'—';$('resetTouchedCount').textContent=sum.touched_wait_rise??'—';$('resetNear5Count').textContent=sum.within_5pt??'—';$('resetWatchCount').textContent=sum.watch_count??rows.length;let visible=[...rows].sort((a,b)=>{let pa={ACTIVE_POSITION:0,SIGNAL_TODAY_NEXT_OPEN:1,RSI30_TOUCHED_WAIT_RISE:2,APPROACHING_RSI30:3,NEAR_RSI30:4,WATCHING:5,SIGNAL_OCCURRED:6};return(pa[a.status]??99)-(pa[b.status]??99)||(a.distance_to_30??999)-(b.distance_to_30??999)||String(a.symbol).localeCompare(String(b.symbol))});$('resetWatchRows').innerHTML=visible.map(x=>`<tr><td><b>${esc(resetStatusJa(x.status))}</b></td><td><b>${esc(x.symbol)}</b></td><td>${x.current_rsi14==null?'—':fmt(x.current_rsi14,1)}</td><td>${x.distance_to_30==null?'—':fmt(x.distance_to_30,1)+'pt'}</td><td>${esc(x.theme||'—')}</td><td>${x.theme_rs_pct_day0==null?'—':fmt(x.theme_rs_pct_day0,1)}</td><td>${x.theme_rank_improvement20_day0==null?'—':fmt(x.theme_rank_improvement20_day0,1)+'pt'}</td><td>${x.theme_breadth21_day0==null?'—':fmt(x.theme_breadth21_day0,1)+'%'}</td><td>${x.day0_rs63_rank==null?'—':'#'+x.day0_rs63_rank}</td><td>${x.current_theme_rs63_top3?'YES':'NO'}</td><td>${x.signal_window_days_left??'—'}</td></tr>`).join('')||'<tr><td colspan="11"><div class="empty">現在、採用Reset条件を通過したRSI30接近銘柄はありません。</div></td></tr>'}
+function renderAllocation(){let g=STATE?.gross100_allocation||{},ready=String(g.status||'').includes('LIVE ALLOCATION READY');$('allocationBadge').textContent=ready?'READY':'DATA REQUIRED';if(!ready){$('allocationPlain').textContent='通常株・Reset・TQQQのどれかのlive入力が不足しているため、最終配分は出していません。欠損を0%扱いしません。';for(const id of['todayNormalAlloc','todayResetAlloc','todayTqqqAlloc','todayGrossAlloc'])$(id).textContent='DATA REQUIRED';return}$('todayNormalAlloc').textContent=pct(g.normal_stock_allocated_pct);$('todayResetAlloc').textContent=pct(g.reset_allocated_pct);$('todayTqqqAlloc').textContent=pct(g.tqqq_allocated_pct);$('todayGrossAlloc').textContent=pct(g.gross_allocated_pct);$('allocationPlain').textContent=`希望配分は 通常株 ${pct(g.normal_stock_desired_pct)} / Reset ${pct(g.reset_desired_pct)} / TQQQ ${pct(g.tqqq_desired_pct)}。競合を調整した最終配分を下に表示しています。`}
 function renderTqqq(){let p=STATE.panic_tqqq||{},n=STATE.normal_tqqq||{};$('tqqqDecision').textContent=n.status==='READY'?`目標 ${pct(n.underlying_target_pct)}`:'判定不可';let active=!!p.active;$('tqqqPlain').textContent=active?'Panic F80が稼働中。CURRENT30の目標を最低80%へ引き上げています。':`Panic F80は未発動。現在はCURRENT30 hierarchyの目標 ${pct(n.underlying_target_pct)}。30%は通常baseで固定値ではありません。`;$('panicPlain').textContent=active?'F80稼働中':'未発動';$('rsiTrigger').textContent=p.rsi4h==null?'DATA REQUIRED':p.touch30_today?'TOUCH30成立':`RSI ${fmt(p.rsi4h)}`;$('mcEntry').textContent=p.mc57==null?'DATA REQUIRED':`${fmt(p.mc57)} / ${p.mc57>=20?'Entry可':'Entry不可'}`;$('seed').textContent=p.seed_age_sessions!=null&&p.seed_age_sessions>=0&&p.seed_age_sessions<=30?'Seed有効':'Seedなし/期限外';$('seedAge').textContent=p.seed_age_sessions==null?'—':`age=${p.seed_age_sessions}`;$('vix').textContent=fmt(p.vix_close,2);$('atrDev').textContent=fmt(p.qqq_sma50_atr_deviation,2);$('dd10').textContent=p.qqq_drawdown10==null?'—':(p.qqq_drawdown10*100).toFixed(2)+'%';$('requestedTarget').textContent=pct(p.requested_target_pct);$('holdDays').textContent=active?Math.max(0,10-(p.held_sessions||0)):'—';$('mcExit').textContent=active&&p.mc57<20?'次回寄りEXIT':'未発動';let g=STATE.gross100_allocation||{},ready=String(g.status||'').includes('LIVE ALLOCATION READY');if(ready){$('resetAllocation').textContent=`${pct(g.reset_desired_pct)} / ${pct(g.reset_allocated_pct)}`;$('tqqqParts').textContent=`${pct(g.tqqq_protected_pct)} / ${pct(g.tqqq_extra_pct)}`;$('normalAllocation').textContent=`${pct(g.normal_stock_desired_pct)} / ${pct(g.normal_stock_allocated_pct)}`;$('executableTarget').textContent=`${pct(g.tqqq_allocated_pct)} / Gross ${pct(g.gross_allocated_pct)}`}}
 function renderRotation(){if(!ROT){$('rotStrong').textContent=$('rotWatch').textContent=$('rotWeak').textContent='Rotationデータ取得失敗。正式V38順位には影響なし。';return}let b=ROT.observations?.rotation_buckets||{};let names=a=>(a||[]).map(x=>x.ticker).join(' / ')||'なし';$('rotStrong').textContent=names(b.mainstream)+'。現在強いという観測で、買いシグナルではありません。';$('rotWatch').textContent=names([...(b.watch||[]),...(b.flow_internal_divergence||[])])+'。先行/乖離の監視。';$('rotWeak').textContent=names([...(b.distribution||[]),...(b.weak_flow_out||[]),...(b.breakdown||[])])+'。警戒Context。Rotation単独で強制売却しません。';if(STATE)renderRanking()}
-async function load(){try{STATE=await fetch('v38-live-state.json',{cache:'no-store'}).then(r=>r.json());$('asof').textContent=`分析基準日 ${STATE.asof} ／ Source: ${STATE.source}`;renderPositions();renderToday();renderTqqq()}catch{$('asof').textContent='V38 state DATA REQUIRED';return}try{ROT=await fetch('https://raw.githubusercontent.com/thanzo12wizu-stack/v38-watchlist/research/rotation-exact-flow-internals-20260831/leadership/research/rotation_daily_observation_brief/daily_observation_brief.json',{cache:'no-store'}).then(r=>r.json());if(ROT.asof!==STATE.asof)ROT=null}catch{ROT=null}try{ROTCTX=await fetch('https://raw.githubusercontent.com/thanzo12wizu-stack/v38-watchlist/research/rotation-exact-flow-internals-20260831/leadership/research/rotation_theme_stock_context/rotation_theme_stock_context.json',{cache:'no-store'}).then(r=>r.json());if(ROTCTX.leadership_coverage?.market_asof!==STATE.asof)ROTCTX=null}catch{ROTCTX=null}renderRotation()}
+async function load(){try{STATE=await fetch('v38-live-state.json',{cache:'no-store'}).then(r=>r.json());$('asof').textContent=`分析基準日 ${STATE.asof} ／ Source: ${STATE.source}`;renderPositions();renderToday();renderTqqq();renderAllocation();renderResetWatch()}catch{$('asof').textContent='V38 state DATA REQUIRED';return}try{ROT=await fetch('https://raw.githubusercontent.com/thanzo12wizu-stack/v38-watchlist/research/rotation-exact-flow-internals-20260831/leadership/research/rotation_daily_observation_brief/daily_observation_brief.json',{cache:'no-store'}).then(r=>r.json());if(ROT.asof!==STATE.asof)ROT=null}catch{ROT=null}try{ROTCTX=await fetch('https://raw.githubusercontent.com/thanzo12wizu-stack/v38-watchlist/research/rotation-exact-flow-internals-20260831/leadership/research/rotation_theme_stock_context/rotation_theme_stock_context.json',{cache:'no-store'}).then(r=>r.json());if(ROTCTX.leadership_coverage?.market_asof!==STATE.asof)ROTCTX=null}catch{ROTCTX=null}renderRotation()}
 load();
 </script></body></html>
--- a/.github/workflows/dashboard.yml
+++ b/.github/workflows/dashboard.yml
@@ -17,6 +17,8 @@
       - 'build_v38_companion.py'
       - 'build_v38_strict_loo_live.py'
       - 'build_v38_tqqq_live.py'
+      - 'build_v38_sleeve_live.py'
+      - 'v38-normal-sleeve-seed.json'
       - 'command-center-v38.html'
       - 'v38_rules.py'
       - 'scripts/export_public_site.py'
@@ -48,6 +50,7 @@
         command-center.html
         command-center_share.html
         v38-live-state.json
+        v38-sleeve-state.json
         v38-strict-loo-history.json
         v38-strict-loo-live.json
         tqqq-panic-state.json
@@ -90,14 +93,15 @@
 
       - name: Preflight tests
         run: |
-          python -m py_compile build_dashboard.py build_v38_companion.py build_v38_strict_loo_live.py build_v38_tqqq_live.py v38_rules.py
+          python -m py_compile build_dashboard.py build_v38_companion.py build_v38_strict_loo_live.py build_v38_tqqq_live.py build_v38_sleeve_live.py v38_rules.py
           pytest -q tests/test_dashboard_build_regressions.py \
             tests/test_options_positioning_quality.py \
             tests/test_eps_acceleration.py \
             tests/test_v38_rules.py \
             tests/test_v38_companion.py \
             tests/test_v38_strict_loo_live.py \
-            tests/test_v38_tqqq_live.py
+            tests/test_v38_tqqq_live.py \
+            tests/test_v38_sleeve_live.py
 
       # TQQQ uses its own small request budget. Fetch it before the legacy
       # dashboard and strict LOO issue thousands of Yahoo requests on this runner.
@@ -213,7 +217,7 @@
             --cache v38-tqqq-live-source-cache.json \
             --out tqqq-panic-state.json
 
-      - name: Build isolated audited V38 companion
+      - name: Build provisional audited V38 companion
         if: steps.build.outputs.skipped != 'true'
         run: |
           python build_v38_companion.py \
@@ -223,6 +227,28 @@
             --tqqq-panic tqqq-panic-state.json \
             --out v38-live-state.json
 
+      - name: Build Normal Stock and RSI Reset live sleeves
+        if: steps.build.outputs.skipped != 'true'
+        run: |
+          python build_v38_sleeve_live.py \
+            --companion v38-live-state.json \
+            --state v38-sleeve-state.json \
+            --seed v38-normal-sleeve-seed.json \
+            --strict-history v38-strict-loo-history.json \
+            --tqqq-state tqqq-panic-state.json \
+            --out v38-sleeve-state.json
+
+      - name: Rebuild audited V38 companion with live allocation inputs
+        if: steps.build.outputs.skipped != 'true'
+        run: |
+          python build_v38_companion.py \
+            --legacy command-center.html \
+            --universe universe.csv \
+            --strict-loo v38-strict-loo-live.json \
+            --tqqq-panic tqqq-panic-state.json \
+            --sleeve-state v38-sleeve-state.json \
+            --out v38-live-state.json
+
       - name: Report skipped stale run
         if: steps.build.outputs.skipped == 'true'
         shell: bash
@@ -250,6 +276,7 @@
               "command-center_share.html": 2_500,
               "command-center-v38.html": 8_000,
               "v38-live-state.json": 1_000,
+              "v38-sleeve-state.json": 200,
               "v38-strict-loo-live.json": 100,
               "tqqq-panic-state.json": 100,
           }
@@ -309,6 +336,20 @@
               if tqqq.get("underlying_target_pct") is None or tqqq.get("requested_target_pct") is None:
                   raise SystemExit("READY TQQQ route is missing CURRENT30/Stage56 targets")
 
+          sleeve = json.loads(Path("v38-sleeve-state.json").read_text(encoding="utf-8"))
+          if sleeve.get("schema") != "v38-sleeve-live-1":
+              raise SystemExit("invalid V38 sleeve live schema")
+          if sleeve.get("asof") != state_date:
+              raise SystemExit(f"V38 sleeve date mismatch: {sleeve.get('asof')} != {state_date}")
+          if sleeve.get("status") == "READY":
+              for key in ("normal_stock", "rsi_reset"):
